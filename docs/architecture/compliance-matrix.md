# Матрица соответствия

## B1. Запуск и воспроизводимость

| ID задания | ID критерия | Проблема/риск               | Где реализовано                                       | Как проверяется                                                                                                                                           | Какие данные нужны | Статус |
| ---------- | ----------- | --------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------ |
| B1         | B1-1        | Предусловия не описаны      | `docs/architecture/runbook.md` §1-2                   | Ревью документа: предусловия (Docker 24+), host-порт gateway (80), внутренние порты infra не публикуются, env (все с дефолтами), expected output         | —                  | готово |
| B1         | B1-2        | Команды старта неоднозначны | `docs/architecture/runbook.md` §3                     | Выполнить `docker compose up -d --build` — единственная команда                                                                                           | Чистая среда       | готово |
| B1         | B1-3        | Скрытые ручные шаги         | `docker-compose.yml`, `Dockerfile`                    | Поднять стек строго по runbook без дополнительных действий. Bootstrap (postgres, clickhouse, kafka topics) выполняется автоматически через init-контейнеры | Дефолтные env      | готово |
| B1         | B1-4        | Сервисы не стартуют         | `docker-compose.yml` (healthchecks на каждом сервисе) | `docker compose ps` — все 5 app-сервисов (control-api, decide-api, ingest-api, control-workers, ingest-workers) + gateway healthy                         | Запущенный стек    | готово |
| B1         | B1-5        | Нет сквозного сценария      | `docs/architecture/demo-scenarios.md` §S1             | Прогон S1 curl-команд: login → create event types → create metric → create flag → create experiment → submit → approve → start → decide → ingest → report | Тестовые данные S1 | готово |

## B2. Feature Flags и выдача вариантов

| ID задания | ID критерия | Проблема/риск                                | Где реализовано                                                                            | Как проверяется                                                       | Какие данные нужны                                                                    | Статус |
| ---------- | ----------- | -------------------------------------------- | ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------ |
| B2         | B2-1        | Без эксперимента не возвращается default     | `src/apps/decide-api/domain/decide.service.ts`                                             | `test/e2e/flags-experiments-runtime.e2e.spec.ts`                      | Флаг без RUNNING эксперимента, `POST /decide` → ожидаем default value                 | готово |
| B2         | B2-2        | Пользователь вне таргетинга получает вариант | `src/apps/decide-api/domain/decide.service.ts`, `src/shared/domain/targeting/evaluator.ts` | `test/e2e/flags-experiments-runtime.e2e.spec.ts`                      | Эксперимент с targeting rule (country=KZ), subject с country=US                       | готово |
| B2         | B2-3        | Не возвращается вариант при применимости     | `src/apps/decide-api/domain/decide.service.ts`                                             | `test/e2e/flags-experiments-runtime.e2e.spec.ts`                      | RUNNING эксперимент, подходящий subject → ожидаем variant value                       | готово |
| B2         | B2-4        | Недетерминизм раздачи                        | `src/apps/decide-api/domain/bucket-calculator.ts`                                          | `test/unit/apps/decide-api/domain/decision/bucket-calculator.spec.ts` | Один subjectId, неизменная конфигурация, 5+ повторных запросов → одинаковый результат | готово |
| B2         | B2-5        | Веса не соблюдаются                          | `src/apps/decide-api/domain/bucket-calculator.ts`                                          | `test/unit/apps/decide-api/domain/decision/bucket-calculator.spec.ts` | 100+ subjectId для проверки распределения ~50/50 ±15%                                 | готово |

## B3. Эксперименты: жизненный цикл и ревью

| ID задания | ID критерия | Проблема/риск                    | Где реализовано                                                                                                    | Как проверяется                           | Какие данные нужны                                                    | Статус |
| ---------- | ----------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- | --------------------------------------------------------------------- | ------ |
| B3         | B3-1        | Нет перехода draft → in_review   | `src/apps/control-api/domain/experiment/experiment.aggregate-root.ts`                                              | `test/e2e/access-review.e2e.spec.ts`      | Эксперимент в DRAFT, `POST /experiments/:id/submit`                   | готово |
| B3         | B3-2        | Нет in_review → approved         | `src/apps/control-api/domain/experiment/services/experiment-review.service.ts`                                     | `test/e2e/access-review.e2e.spec.ts`      | Аппрувер-группа с порогом, `POST /experiments/:id/approve`            | готово |
| B3         | B3-3        | Старт без одобрений              | `src/apps/control-api/domain/experiment/services/experiment-review.service.ts`                                     | `test/e2e/access-review.e2e.spec.ts`      | Порог > фактических одобрений → `POST /experiments/:id/start` → 4xx   | готово |
| B3         | B3-4        | Допускаются запрещённые переходы | `src/apps/control-api/domain/experiment/experiment.aggregate-root.ts`, `errors/invalid-status-transition.error.ts` | `test/e2e/criteria-hardening.e2e.spec.ts` | Набор запрещённых переходов (running→draft, completed→running и т.д.) | готово |
| B3         | B3-5        | Ревью доступно не тем ролям      | `src/apps/control-api/domain/experiment/services/experiment-review.service.ts`                                     | `test/e2e/access-review.e2e.spec.ts`      | VIEWER вызывает approve → 403; EXPERIMENTER без назначения → отказ    | готово |

## B4. События и атрибуция

| ID задания | ID критерия | Проблема/риск                              | Где реализовано                                                                                                                                                                                                                                                                                                 | Как проверяется                                                                                | Какие данные нужны                                                                                        | Статус |
| ---------- | ----------- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------ |
| B4         | B4-1        | Невалидные типы полей принимаются          | Структурная: `src/apps/ingest-api/presentation/dto/ingest-events.dto.ts` (Zod: типы полей, regex, min/max). Семантическая: `src/apps/ingest-api/application/ingest-events.use-case.ts` (unknown type, archived type). Payload schema: `src/apps/ingest-api/domain/event-payload.validator.ts` (Ajv JSON Schema) | `test/e2e/events-ingest.e2e.spec.ts`                                                           | Невалидные payload'ы: wrong field types, unknown event type key, payload not matching JSON schema         | готово |
| B4         | B4-2        | События без обязательных полей принимаются | DTO Zod: `src/apps/ingest-api/presentation/dto/ingest-events.dto.ts` (eventId, eventTypeKey, decisionId, subjectId, timestamp — required, min(1)). Semantic: `src/apps/ingest-api/application/ingest-events.use-case.ts` (decision token verification, subject mismatch check)                                  | `test/e2e/events-ingest.e2e.spec.ts`                                                           | Событие без eventId, без timestamp, без decisionId → rejected                                             | готово |
| B4         | B4-3        | Дубликаты учитываются повторно             | In-batch: `src/apps/ingest-api/application/ingest-events.use-case.ts` (seenEventIds Set). Cross-batch: `src/apps/ingest-workers/deduplication.consumer.ts` (Redis `SET NX` `idem:event:{eventId}`, TTL 7 дней)                                                                                                  | `test/e2e/events-contract.e2e.spec.ts`, `test/integration/events-pipeline.integration.spec.ts` | Пакет с повторяющимся eventId (in-batch → duplicates:1), повторная отправка того же eventId (cross-batch) | готово |
| B4         | B4-4        | Exposure не связан с decisionId            | `src/apps/ingest-api/application/ingest-events.use-case.ts` — JWT decision token верифицируется через `DecisionTokenSigner`, experimentId/variantId/subjectId извлекаются из payload (поля `e`, `v`, `u`). subjectId из токена сверяется с subjectId из запроса                                                 | `test/e2e/events-ingest.e2e.spec.ts`                                                           | Валидный decision token (из /decide), невалидный токен → rejected                                         | готово |
| B4         | B4-5        | Конверсия без exposure учитывается         | `src/apps/ingest-workers/attribution.consumer.ts` — проверяет Redis `attr:exposure:{decisionId}`; без exposure → pending queue (`attr:pending:{decisionId}` Redis List, TTL 7 дней). При появлении exposure → `LRANGE` + `DEL` pending → flush attributed events                                                          | `test/e2e/events-attribution-ordering.e2e.spec.ts`                                             | Out-of-order: click приходит раньше exposure → pending. Exposure приходит позже → flush + attributed      | готово |

## B5. Устойчивость и safety-механики

| ID задания | ID критерия | Проблема/риск                          | Где реализовано                                                                                                                                                                                                                                                                                                                                                                                       | Как проверяется                                                                                                               | Какие данные нужны                                                                                                                    | Статус |
| ---------- | ----------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| B5         | B5-1        | Guardrail без metric_key               | `src/apps/control-api/presentation/experiment/dto/create-guardrail-rule.dto.ts` — Zod validation, metricId required                                                                                                                                                                                                                                                                                   | `test/e2e/guardrails.e2e.spec.ts`                                                                                             | POST guardrail без metricId → 4xx validation error                                                                                    | готово |
| B5         | B5-2        | Guardrail без threshold                | `src/apps/control-api/presentation/experiment/dto/create-guardrail-rule.dto.ts` — Zod validation, threshold required                                                                                                                                                                                                                                                                                  | `test/e2e/guardrails.e2e.spec.ts`                                                                                             | POST guardrail без threshold → 4xx validation error                                                                                   | готово |
| B5         | B5-3        | Превышение порога не обнаруживается    | Оркестрация: `infrastructure/guardrail/guardrails/guardrail-check.service.ts` (`@Cron(EVERY_MINUTE)` + distributed lock Redis). Загрузка правил: `guardrail-rule-loader.service.ts` (PostgreSQL). Вычисление метрики: `infrastructure/metric/persistence/metric-data.clickhouse-source.ts` (SQL к ClickHouse `metric_obs`/`metric_rollups_mv`). Сравнение с порогом: `guardrail-evaluator.service.ts` | `test/e2e/guardrails.e2e.spec.ts`                                                                                             | Metric rollup данные в ClickHouse выше threshold → breach detected                                                                    | готово |
| B5         | B5-4        | Действие guardrail не выполняется      | `infrastructure/guardrail/guardrails/guardrail-action-executor.service.ts` — вызывает `pause()` или rollback на ExperimentAggregateRoot через ExperimentRepository                                                                                                                                                                                                                                    | `test/e2e/guardrails.e2e.spec.ts`                                                                                             | RUNNING эксперимент + breach metric → experiment status = PAUSED или COMPLETED                                                        | готово |
| B5         | B5-5        | Нет аудита срабатывания                | Write: `guardrail-action-executor.service.ts` (INSERT GuardrailTrigger в PostgreSQL). Read: `guardrail-trigger.read-prisma-repository.ts`. API: `experiment-guardrail-trigger.controller.ts` (`GET /experiments/:id/guardrail-triggers`). Schema: `prisma/schema/guardrails.prisma`                                                                                                                   | `test/e2e/guardrails.e2e.spec.ts`                                                                                             | После breach → `GET /experiments/:id/guardrail-triggers` → data[] с id, guardrailId, metricValue, threshold, actionTaken, triggeredAt | готово |
| B5         | B5-6        | Пользователь постоянно в экспериментах | Policy: `src/apps/decide-api/domain/subject-participation/participation-policy.ts`. State: `src/apps/decide-api/infrastructure/subject-participation/redis-participation-limiter.ts` (Redis counters, configurable: max concurrent, cooldown after total, rolling window, cooldown period)                                                                                                            | `test/e2e/flags-experiments-runtime.e2e.spec.ts`, `test/unit/shared/infrastructure/cache/redis-participation-limiter.spec.ts` | Repeated decide calls для одного subjectId → после лимита → default values                                                            | готово |

## B6. Отчётность и принятие решения

| ID задания | ID критерия | Проблема/риск              | Где реализовано                                                                                                                                                                                                                           | Как проверяется                         | Какие данные нужны                                                                                    | Статус |
| ---------- | ----------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------ |
| B6         | B6-1        | Нет фильтра по периоду     | `src/apps/control-api/presentation/report/dto/get-experiment-report.query.dto.ts` — Zod: `from` (required ISO datetime), `to` (required ISO datetime), `bucket` (optional, enum minute/hour/day, default 'minute'). Validation: from < to | `test/e2e/reports-outcomes.e2e.spec.ts` | `GET /reports/experiments/:id?from=...&to=...` — отчёт пересчитывается по периоду                     | готово |
| B6         | B6-2        | Нет разреза по вариантам   | `src/apps/control-api/application/report/queries/get-experiment-report/get-experiment-report.use-case.ts`                                                                                                                                 | `test/e2e/reports-outcomes.e2e.spec.ts` | Эксперимент с 2+ variants → отчёт содержит метрики для каждого варианта отдельно                      | готово |
| B6         | B6-3        | Не показываются метрики    | `get-experiment-report.use-case.ts` — загружает metric definitions из PostgreSQL (`MetricReadRepository`), вычисляет через `MetricDataClickHouseSource` (SQL к ClickHouse) + `MetricFormulaEvaluator`                                     | `test/e2e/reports-outcomes.e2e.spec.ts` | Эксперимент с attached metrics (primaryMetricId + metricIds) → метрики присутствуют в report response | готово |
| B6         | B6-4        | Нельзя зафиксировать исход | `src/apps/control-api/domain/experiment/enums/experiment-outcome-type.enum.ts` (ROLLOUT_WINNER, ROLLBACK, NO_EFFECT). `experiment.aggregate-root.ts` → `complete()` method                                                                | `test/e2e/reports-outcomes.e2e.spec.ts` | `POST /experiments/:id/complete` с outcomeType → статус COMPLETED                                     | готово |
| B6         | B6-5        | Решение без обоснования    | `src/apps/control-api/domain/experiment/experiment.aggregate-root.ts` — `complete()` throws `CompletionCommentRequiredError` если comment пуст                                                                                            | `test/e2e/reports-outcomes.e2e.spec.ts` | Complete без comment → 4xx; complete с comment → success                                              | готово |

## B7. Архитектура и ориентируемость

| ID задания | ID критерия | Проблема/риск                    | Где реализовано                                                                                                                                                                                                                                                                    | Как проверяется                                                                                                                              | Какие данные нужны | Статус |
| ---------- | ----------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | ------ |
| B7         | B7-1        | Непрозрачный нейминг             | `src/apps/*/presentation/*/dto/*.dto.ts` — все DTO названы по действию (CreateFlagDto, DecideRequestDto, IngestEventsRequestDto и т.д.). Контроллеры: `/flags`, `/experiments`, `/metric-definitions`, `/event-types`, `/decide`, `/events/ingest`                                 | Ревью кода: названия эндпоинтов, DTO, сущностей                                                                                              | —                  | готово |
| B7         | B7-2        | Смешанные границы модулей        | `test/architecture/microservice-boundaries.spec.ts`                                                                                                                                                                                                                                | `bun run lint:arch` — 0 failures                                                                                                             | —                  | готово |
| B7         | B7-3        | Нет трассируемости               | `docs/architecture/compliance-matrix.md` (этот файл)                                                                                                                                                                                                                               | Ревью: все B1-B10 + FX строки заполнены с ссылками на код, тесты, данные                                                                     | —                  | готово |
| B7         | B7-4        | Не зафиксированы ADR             | `docs/architecture/architecture-decisions.md` — 13 ADR: микросервисы, fail-closed, Kafka pipeline, rollups, participation Redis, guardrail rollback, event sourcing, JWT decision, Redis snapshots, sequential pipeline, ClickHouse Kafka Engine, outbox pattern, distributed lock | Ревью документа                                                                                                                              | —                  | готово |
| B7         | B7-5        | Нет C4 Context                   | `docs/architecture/c4/context.mmd`                                                                                                                                                                                                                                                 | Ревью: 4 персоны (Admin, Experimenter, Approver, Viewer) + Product App + Platform + Telegram/Slack                                           | —                  | готово |
| B7         | B7-6        | Нет C4 Container                 | `docs/architecture/c4/container.mmd`                                                                                                                                                                                                                                               | Ревью: 5 app services + gateway + PostgreSQL/ClickHouse/Redis/Kafka + observability. Реальные маршруты, топики, Redis keys                   | —                  | готово |
| B7         | B7-7        | Нет C4 Component критичного пути | `docs/architecture/c4/component-decide-event-report.mmd`                                                                                                                                                                                                                           | Ревью: decide → event → report/guardrail path. Все 5 сервисов, sequential ingest pipeline, ClickHouse Kafka Engine, Redis snapshot transport | —                  | готово |
| B7         | B7-8        | Нет карты репозитория            | `docs/architecture/repo-map.md`                                                                                                                                                                                                                                                    | Ревью: entry points, app structure, shared code, contracts, 6 Kafka topics, Redis keys, infrastructure, tests                                | —                  | готово |
| B7         | B7-9        | Не зафиксированы ограничения     | `docs/architecture/limitations.md` — 13 пунктов                                                                                                                                                                                                                                    | Ревью документа                                                                                                                              | —                  | готово |

## B8. Тестирование

| ID задания | ID критерия | Проблема/риск             | Где реализовано                                                                                                                                                            | Как проверяется                                                | Какие данные нужны                      | Статус |
| ---------- | ----------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------- | ------ |
| B8         | B8-1        | Нет негативных тестов     | `test/e2e/edge-cases.e2e.spec.ts` (forbidden transitions, invalid data), `test/e2e/events-ingest.e2e.spec.ts` (unknown type, invalid token, missing fields, archived type) | `bun run test:e2e`                                             | Негативные входные данные               | готово |
| B8         | B8-2        | Нет интеграционных тестов | `test/integration/decide-ingest.integration.spec.ts` (decide → ingest pipeline), `test/integration/events-pipeline.integration.spec.ts` (dedup → attribution)              | `bun run test:integration` (требует запущенный стек)           | PostgreSQL + Redis + Kafka + ClickHouse | готово |
| B8         | B8-4        | Нет отчёта с покрытием    | `docs/architecture/testing-report.md`, `coverage/lcov.info`                                                                                                                | `bun run test:cov -- --coverage.reporter=lcov --reporter=dot`  | —                                       | готово |

## B9. Эксплуатационная готовность и наблюдаемость

| ID задания | ID критерия | Проблема/риск                  | Где реализовано                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Как проверяется                                                                                                                                           | Какие данные нужны   | Статус |
| ---------- | ----------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------ |
| B9         | B9-1        | Readiness не работает          | control-api: `src/shared/presentation/health/health.controller.ts` (PostgreSQL + Redis + ClickHouse + Kafka). decide-api: `src/apps/decide-api/presentation/decide-health.controller.ts` (Redis + Kafka + RuntimeSnapshot.isReady()). ingest-api: `src/apps/ingest-api/presentation/ingest-health.controller.ts` (ClickHouse + Kafka + EventTypeCatalog.isReady())                                                                                                 | `curl -f http://127.0.0.1/ready` (gateway → control-api). SLA ≤ 180s. Per-service: `docker compose exec decide-api wget -qO- http://localhost:3000/ready` | Запущенный стек      | готово |
| B9         | B9-2        | Нет liveness                   | Каждый сервис: `GET /health` → `{"status":"ok"}` без проверки зависимостей. Gateway: проксируется к control-api `/health` → `{"status":"ok"}`                                                                                                                                                                                                                                                                                                                   | `curl -f http://127.0.0.1/health` (gateway). App liveness: `docker compose exec control-api wget -qO- http://localhost:3000/health`                       | Запущенный стек      | готово |
| B9         | B9-3        | Нет метрик                     | `src/shared/presentation/metrics/metrics.service.ts` — 5 кастомных метрик: `http_requests_total` (Counter, labels: method/path/status), `lotty_decide_total` (Counter), `lotty_ingest_total` (Counter), `lotty_decide_duration_ms` (Histogram), `lotty_active_experiments` (Gauge). Плюс `collectDefaultMetrics()` (Node.js process). `metrics.controller.ts` → `GET /metrics` Prometheus text                                                                     | `curl http://127.0.0.1/metrics` (gateway → control-api). Для decide-api: `docker compose exec decide-api wget -qO- http://localhost:3000/metrics`         | Запущенный стек      | готово |
| B9         | B9-4        | Логи неструктурированные       | `src/shared/infrastructure/logging/pino-config.factory.ts` — Pino JSON. Стабильные поля: event, domain, operation, status, statusCode, durationMs, requestId                                                                                                                                                                                                                                                                                                       | `docker compose logs --tail=5 control-api` — каждая строка = JSON                                                                                         | Запущенный стек      | готово |
| B9         | B9-6        | Не описано поведение при росте | `docs/architecture/operational-readiness.md` §5-6. `perf/k6/smoke-decide-ingest.js`, `perf/k6/decide-p99-stress.js`                                                                                                                                                                                                                                                                                                                                                | Ревью документа + `bun run perf:smoke`                                                                                                                    | Запущенный стек + k6 | готово |
| B9         | B9-7        | Нет мер эффективности хранения | PostgreSQL: unique indexes на flags(key), users(email), experiments(flagId+status), event_types(key); version columns для optimistic locking (`prisma/schema/*.prisma`). ClickHouse: ReplacingMergeTree (events, decisions), MergeTree (metric_obs), AggregatingMergeTree (metric_rollups_mv); PARTITION BY toYYYYMM; TTL 90 дней; Kafka Engine fan-in (`clickhouse-bootstrap.queries.ts`). Redis: TTL на idem/exposure/pending/lock keys; MAXLEN ~1000 на streams | Ревью `prisma/schema/*.prisma`, `clickhouse-bootstrap.queries.ts`                                                                                         | Schema файлы         | готово |

## B10. Инженерная дисциплина

| ID задания | ID критерия | Проблема/риск | Где реализовано                                                                 | Как проверяется           | Какие данные нужны | Статус |
| ---------- | ----------- | ------------- | ------------------------------------------------------------------------------- | ------------------------- | ------------------ | ------ |
| B10        | B10-1       | Нет lint      | `biome.json`, `package.json` → `"lint": "bunx biome check src"`                 | `bun run lint` — 0 errors | —                  | готово |
| B10        | B10-2       | Нет format    | `biome.json`, `package.json` → `"format": "bunx biome format --write src test"` | `bun run format`          | —                  | готово |

## FX. Дополнительные фичи

### C11 — Разруливание конфликтующих экспериментов

| ID задания | ID критерия | Проблема/риск                               | Где реализовано                                                                                                                                                                                             | Как проверяется                                                                                 | Какие данные нужны                                                              | Статус |
| ---------- | ----------- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------ |
| C11        | FX-1        | Конфликтующие эксперименты смешивают эффект | `src/apps/decide-api/application/services/experiment-assignment.service.ts` — mutual exclusion + priority tiers по `conflictDomain`. Побеждает highest priority; проигравший → reason `EXPERIMENT_CONFLICT` | `test/e2e/flags-experiments-runtime.e2e.spec.ts`                                                | Два RUNNING эксперимента на разных флагах, один conflictDomain, разный priority | готово |
| C11        | FX-2        | Tie-break недетерминирован                  | `src/apps/decide-api/application/services/experiment-assignment.service.ts` — deterministic tie-breaker при equal priority (stable sort by experiment id)                                                   | `test/unit/apps/decide-api/application/decision/services/experiment-assignment.service.spec.ts` | Equal-priority experiments в одном conflictDomain → стабильный результат        | готово |

### C7 — Платформа уведомлений

| ID задания | ID критерия | Проблема/риск                               | Где реализовано                                                                                                                                                                                                                                                                                                                                                                                   | Как проверяется                         | Какие данные нужны                                                             | Статус |
| ---------- | ----------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------ | ------ |
| C7         | FX-1        | Lifecycle/guardrail события не доставляются | Pipeline: `OutboxRelayService` → Kafka `control.domain-events` → `notification.consumer.ts` → `notification.dispatcher.ts` (rule matching по event/scope, dedup by `dedupeWindowSec`, rate-limit by `rateLimitCount`/`rateLimitWindowSec`) → `notification.renderer.ts` (template rendering) → `telegram.sender.ts` / `slack.sender.ts`. Deliveries persist: `prisma/schema/notifications.prisma` | `test/e2e/notifications-c7.e2e.spec.ts` | Channel + rule + experiment с guardrail trigger → delivery record в PostgreSQL | готово |
| C7         | FX-2        | Шум уведомлений не ограничен                | `src/apps/control-workers/notification/notification.dispatcher.ts` — `dedupeWindowSec` (suppress same eventName within window per rule+target), `rateLimitCount` + `rateLimitWindowSec` (max N SENT deliveries per window per rule+target). Statuses: SENT, SUPPRESSED_DEDUP, SUPPRESSED_RATE_LIMIT, FAILED                                                                                       | `test/e2e/notifications-c7.e2e.spec.ts` | Два trigger-события в пределах rateLimitWindowSec → второе suppressed          | готово |

### C9 — Learnings Library

| ID задания | ID критерия | Проблема/риск                            | Где реализовано                                                                                                                                                                                                                                   | Как проверяется                     | Какие данные нужны                                                                                    | Статус |
| ---------- | ----------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------- | ------ |
| C9         | FX-1        | Learnings не сохраняются / не аудируются | CRUD: `src/apps/control-api/presentation/learning/learning.controller.ts` (`@Controller('learnings')`). Domain: `learning-entry.aggregate-root.ts`. Schema: `prisma/schema/learnings.prisma`. Endpoints: POST, GET, PATCH, GET list, POST archive | `test/e2e/learnings-c9.e2e.spec.ts` | Experiment fixture, создание learning с hypothesis/summary/tags, обновление, проверка аудита          | готово |
| C9         | FX-2        | Нельзя найти похожие эксперименты        | `src/apps/control-api/application/learning/queries/find-similar-learnings/find-similar-learnings.use-case.ts` — поиск по featureKey, tags, primaryMetricKey, team, countries, platforms. Endpoint: `GET /learnings/similar?experimentId=...`      | `test/e2e/learnings-c9.e2e.spec.ts` | Base learning + similar (same featureKey/tags) + unrelated learning → similar returned, unrelated not | готово |
